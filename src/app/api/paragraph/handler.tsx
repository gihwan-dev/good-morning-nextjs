import { connectToDatabase } from "@/lib/db";
import { CreateParagraph } from "./route";

export async function createParagraphHandler({ text }: CreateParagraph) {
  // RMDBMS 사용 예정. PostgreSQL 사용 예정.
}
